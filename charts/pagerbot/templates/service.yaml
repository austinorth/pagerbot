apiVersion: v1
kind: Service
metadata:
  name: {{ include "pagerbot.fullname" . }}
  labels:
    {{- include "pagerbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pagerbot.selectorLabels" . | nindent 4 }}
